VETRA 4.1.0 — INSTANT SAVE FIX

1. Extract this ZIP into the repository root and replace the old files.
2. Keep Vercel Root Directory blank.
3. Commit and push all files.
4. Promote the newest deployment to Production if needed.
5. Hard-refresh both admin and storefront tabs.

VERIFY
- Admin header says Admin · v4.1.0.
- The admin header shows Saving… then Saved after every change.
- Keep the storefront open in another tab: saved text and image changes should refresh there automatically.
- document.body.dataset.vetraVersion returns 4.1.0.

Existing browser content migrates automatically to IndexedDB. Do not clear site data unless you intentionally want to erase saved content.
